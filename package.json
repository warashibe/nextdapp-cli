{
  "name": "nextdapp",
  "version": "0.1.26",
  "main": "./lib/index.js",
  "bin": "bin/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "cross-env BABEL_ENV=production babel src --out-dir lib && cross-env BABEL_ENV=production babel src-bin --out-dir bin",
    "test": "mocha --require babel-core/register --require babel-polyfill --recursive",
    "test:watch": "npm test -- --watch"
  },
  "dependencies": {
    "command-exists": "^1.2.9",
    "fs-extra": "^9.0.0",
    "ramdam": "^1.6.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-node": "^0.0.1-security",
    "babel-plugin-add-module-exports": "^1.0.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.5.0",
    "cross-env": "^5.1.3",
    "mocha": "^6.1.3",
    "rimraf": "^2.6.2"
  }
}
